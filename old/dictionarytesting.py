my_dict = {
  "countries": ["France", "Germany", "Italy"],
  "capitals": ["Paris", "Berlin", "Rome"]
}

# Convert the dictionary to JSON string (optional)
import json
json_string = json.dumps(my_dict)
print(json_string)